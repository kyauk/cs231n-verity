# Core pipeline
numpy

# LIBERO simulation stack (required for --render / preview generation)
mujoco
robosuite==1.4.0
bddl==1.0.1
easydict
termcolor
cloudpickle
gym==0.25.2
future
matplotlib
einops

# Image processing (requires system ImageMagick: brew install imagemagick)
wand
scikit-image

# PyTorch (needed for loading init state files)
torch

# Web UI
fastapi
uvicorn[standard]
python-multipart
jinja2

# LIBERO-plus is installed in editable mode separately:
#   cd LIBERO-plus-main && pip install -e .
