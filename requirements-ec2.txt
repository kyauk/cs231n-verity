-r requirements.txt
boto3
streamlit
requests
# Edge Case Discovery Engine -- EC2 runtime dependencies
# NV-DINOv2 runs via NIM API (no torch/GPU needed for embedding).

# nuPlan devkit
nuplan-devkit

# Database
psycopg[binary]
pgvector

# Async HTTP for NIM API calls
httpx
aiohttp

# OpenAI-compatible client for NIM
openai

# Data / numerics
numpy
Pillow

# Clustering pipeline
scikit-learn
umap-learn
hdbscan

# Plotting (Phase 2 visualization)
matplotlib

# Config
python-dotenv
pydantic

# FastAPI (Phase 6)
fastapi
uvicorn[standard]

# AWS S3 streaming
boto3
