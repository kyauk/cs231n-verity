# Waymo discovery pipeline + runner dependencies.

# --- API server ---
fastapi
uvicorn[standard]
pydantic
python-dotenv
python-multipart  # required for the debate test UI's video upload form

# --- pipeline stages ---
requests
numpy
opencv-python-headless
pyarrow
pandas
tqdm

# --- GCS access (Waymo Open Dataset + segment videos) ---
gcsfs
google-auth
google-cloud-storage

# --- clustering ---
umap-learn
hdbscan
scikit-learn

# --- pgvector population ---
psycopg2-binary
